# Apply hides Terraform

The product is `relay apply -f`. Terraform is an engine under a local workdir, not the consumer contract. Service teams do not review or run `.tf`. Generate-only was rejected because a PaaS that stops at files still makes the developer operate the pipeline.
